import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
import {styles,guides,catalog,version,revision,origin,projects,guideMarkdown,briefMarkdown,effortText,contrast} from './model.mjs';
import {renderScenario} from '../../public/territories/assets/scenarios.js';
const root=fileURLToPath(new URL('../../',import.meta.url));
const e=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const json=x=>JSON.stringify(x,null,2)+'\n';
function write(relative,content){const target=path.join(root,'public',relative);fs.mkdirSync(path.dirname(target),{recursive:true});if(!fs.existsSync(target)||!fs.readFileSync(target).equals(Buffer.from(content)))fs.writeFileSync(target,content);}
const shell=(title,pathname,body,script='')=>`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${e(title)} · Territories</title><meta name="description" content="Choose a visual direction, compare working examples, and hand an agent a precise build brief."><link rel="canonical" href="${origin+pathname}"><link rel="stylesheet" href="/territories/assets/site.css">${script?`<script type="module" src="/territories/assets/${script}"></script>`:''}</head><body><a class="skip" href="#main">Skip to content</a><header class="shell"><a href="/projects/territories/">← Territories</a><a href="/territories/start/" ${path==='/territories/start/'?'aria-current="page"':''}>Find a direction</a><a href="/territories/compare/" ${path==='/territories/compare/'?'aria-current="page"':''}>Compare</a><a href="/territories/agents/" ${path==='/territories/agents/'?'aria-current="page"':''}>For agents</a></header><main id="main">${body}</main><footer>Made by <a href="/about/">Jonathan Christensen</a> · Static, public, no account required. <a href="/territories/catalog.json">Catalog JSON</a></footer></body></html>`;
const list=(items,tag='ul')=>`<${tag}>${items.map(x=>`<li>${e(x)}</li>`).join('')}</${tag}>`;
const tokenCSS=t=>`--surface:${t.color.surface};--text:${t.color.text};--accent:${t.color.accent};--on-accent:${t.color.onAccent};--page:${t.spacing.page};--section:${t.spacing.section};--radius:${t.border.radius};--border-width:${t.border.width};--shadow:${t.shadow};--display-family:${t.typography.display.family};--display-min:${t.typography.display.size};--display-max:${t.typography.display.maxSize};--display-leading:${t.typography.display.lineHeight};--display-weight:${t.typography.display.weight}`;
const facts=s=>`<dl class="facts"><div><dt>Reading & layout</dt><dd>${s.profile.density==='compact'?'Compact spacing for scanning':'Open spacing for a slower reading pace'}</dd></div><div><dt>Visual emphasis</dt><dd>${e(s.profile.tone)} · ${e(s.profile.expression)}</dd></div><div><dt>Implementation</dt><dd>${e(effortText(s.profile.effort))}</dd></div><div><dt>Images & assets</dt><dd>${e(s.profile.imagery)}</dd></div></dl>`;
const choices=(selected='')=>styles.map(s=>`<option value="${s.id}"${s.id===selected?' selected':''}>${e(s.name)}</option>`).join('');
const projectSelect=Object.entries(projects).map(([id,name])=>`<option value="${id}">${name}</option>`).join('');
const fields=`<div class="picker-fields"><label class="field"><span>What are you making?</span><select name="project">${projectSelect}</select></label><label class="field"><span>How should it feel?</span><select name="tone"><option value="any">Open to either</option><option value="quiet">Quiet</option><option value="bold">Bold</option></select></label><label class="field"><span>How much fits on screen?</span><select name="density"><option value="any">No preference</option><option value="compact">More at a glance</option><option value="spacious">More breathing room</option></select></label><label class="field"><span>Asset work</span><select name="assets"><option value="any">Open to custom artwork</option><option value="simple">Mostly type and CSS</option></select></label></div>`;
const agentDoc=`# Territories for agents\n\nA public static design reference at ${origin}. No API key or MCP connection is required.\n\n1. Read [catalog.json](/territories/catalog.json) for all 50 styles, project suggestions, practical tradeoffs, and resource links.\n2. Read the selected style's guide.md, tokens.json, and checklist.md. A brief.md provides a condensed handoff.\n3. Inspect its example/ URL with ?project=portfolio, internal-tool, store, or publication when browser access is available. Compare the same content at [Compare](/territories/compare/).\n4. Apply the direction within the user's requested scope; test using the checklist.\n\nAll root-relative URLs resolve against the host used to fetch this document. In local review, retain the preview origin. Downloads work when an agent cannot fetch URLs.\n\n## Contract\n\nSchema version ${version}. Content revision ${revision}. The current aliases under /territories/{id}/ track the latest compatible recipe. /territories/v1/{id}/guide.md and tokens.json expose the v1 contract. Revisions may change values or prose without changing the schema. Record revision when reproducibility matters.\n\nThe token format is a small Territories-specific semantic schema, not a claim of compatibility with another token standard. See [schema.json](/territories/schema.json). Color pairs are for opaque surfaces; transparency and artwork need separate checks.\n\n## Selection\n\nProject and preference matches are editorial heuristics, not objective quality scores. Styles remain available outside their suggested projects. Functional examples are original CSS studies; image-led directions need assets to achieve their full expression.\n\n## Skill\n\n[Read SKILL.md](/territories/skill/SKILL.md) or [download the skill folder](/territories/territories-design.zip). Installing it is optional. It adds a workflow, not tools or permissions.\n\n## Discovery\n\n[llms.txt](/llms.txt) · [Catalog](/territories/catalog.json) · [All guide text](/territories/guides.md)\n`;
// Minimal uncompressed ZIP keeps the downloadable skill dependency-free.
function zip(entries){const local=[],central=[];let offset=0;const crc=buf=>{let n=0xffffffff;for(const b of buf){n^=b;for(let i=0;i<8;i++)n=(n>>>1)^((n&1)?0xedb88320:0);}return (n^0xffffffff)>>>0;};for(const [name,text] of entries){const file=Buffer.from(name),data=Buffer.from(text),sum=crc(data),h=Buffer.alloc(30);h.writeUInt32LE(0x04034b50);h.writeUInt16LE(20,4);h.writeUInt16LE(33,12);h.writeUInt32LE(sum,14);h.writeUInt32LE(data.length,18);h.writeUInt32LE(data.length,22);h.writeUInt16LE(file.length,26);local.push(h,file,data);const c=Buffer.alloc(46);c.writeUInt32LE(0x02014b50);c.writeUInt16LE(20,4);c.writeUInt16LE(20,6);c.writeUInt16LE(33,14);c.writeUInt32LE(sum,16);c.writeUInt32LE(data.length,20);c.writeUInt32LE(data.length,24);c.writeUInt16LE(file.length,28);c.writeUInt32LE(offset,42);central.push(c,file);offset+=h.length+file.length+data.length;}const directory=Buffer.concat(central),end=Buffer.alloc(22);end.writeUInt32LE(0x06054b50);end.writeUInt16LE(entries.length,8);end.writeUInt16LE(entries.length,10);end.writeUInt32LE(directory.length,12);end.writeUInt32LE(offset,16);return Buffer.concat([...local,directory,end]);}
export function generateTerritories(){
 write('projects/territories/guide-data.js','// Generated from src/data/territories/guides.json.\nconst guideData = '+JSON.stringify(guides,null,2)+';\n');
 write('territories/catalog.json',json(catalog));write('territories/v1/catalog.json',json(catalog));
 write('territories/agent.md',agentDoc);write('territories/guides.md',styles.map(guideMarkdown).join('\n---\n\n'));
 write('llms.txt',`# Jonathan Christensen\n\nPersonal projects and practical design references.\n\n## Territories\n- [Agent instructions](${origin}/territories/agent.md): How to choose and apply a direction.\n- [Catalog](${origin}/territories/catalog.json): 50 styles with machine-readable links and tradeoffs.\n- [Compare](${origin}/territories/compare/): Same functional example in different directions.\n- [Skill](${origin}/territories/skill/SKILL.md): Optional agent workflow.\n`);
 const schema={$schema:'https://json-schema.org/draft/2020-12/schema',$id:origin+'/territories/schema.json',title:'Territories tokens v1',type:'object',required:['schemaVersion','revision','styleId','name','scope','color','typography','spacing','border','shadow','layout','motion','decoration'],properties:{schemaVersion:{const:version},revision:{type:'string'},styleId:{type:'string',enum:styles.map(s=>s.id)},name:{type:'string'},scope:{type:'string'},color:{type:'object',required:['surface','text','accent','onAccent','border'],additionalProperties:{type:'string',pattern:'^#[0-9a-fA-F]{6}$'}},typography:{type:'object',required:['display','heading','body','caption'],additionalProperties:{type:'object',required:['family','size','weight','lineHeight'],properties:{family:{type:'string'},size:{type:'string'},weight:{type:'number'},lineHeight:{type:'number'}}}},spacing:{type:'object',required:['unit','control','section','page'],additionalProperties:{type:'string'}},border:{type:'object',required:['width','radius'],additionalProperties:{type:'string'}},shadow:{type:'string'},layout:{type:'object',required:['readingMeasure','collapseAt','minimumControlHeight'],additionalProperties:{type:'string'}},motion:{type:'object',required:['duration','reducedMotion'],additionalProperties:{type:'string'}},decoration:{type:'object',required:['treatment','scope'],additionalProperties:{type:'string'}}}};
 write('territories/schema.json',json(schema));
 const skill=fs.readFileSync(path.join(root,'src/data/territories/skill/SKILL.md'),'utf8'),yaml=fs.readFileSync(path.join(root,'src/data/territories/skill/agents/openai.yaml'),'utf8');write('territories/skill/SKILL.md',skill);write('territories/skill/agents/openai.yaml',yaml);write('territories/territories-design.zip',zip([['territories-design/SKILL.md',skill],['territories-design/agents/openai.yaml',yaml]]));
 for(const s of styles){const dir='territories/'+s.id+'/';const guide=guideMarkdown(s),brief=briefMarkdown(s),check=`# ${s.name}: acceptance checklist\n\n${s.checks.map(x=>'- [ ] '+x).join('\n')}\n\nSchema ${version}; revision ${revision}.\n`;write(dir+'guide.md',guide);write(dir+'brief.md',brief);write(dir+'checklist.md',check);write(dir+'tokens.json',json(s.tokens));write(`territories/v1/${s.id}/tokens.json`,json(s.tokens));write(`territories/v1/${s.id}/guide.md`,guide);
 write(dir+'example/index.html',`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${e(s.name)} · Working example</title><meta name="robots" content="noindex"><link rel="stylesheet" href="/territories/assets/example.css"><style>:root{${tokenCSS(s.tokens)}}</style><script type="module" src="/territories/assets/example.js"></script></head><body class="treatment-${s.profile.treatment} style-${s.id}"><div id="example">${renderScenario('internal-tool')}</div></body></html>`);
 write(dir+'index.html',shell(s.name,s.links.page,`<div class="intro"><p class="eyebrow">${e(s.context)}</p><h1>${e(s.name)}</h1><p>${e(s.overview)}</p><div class="actions"><a class="button" href="/territories/compare/?styles=${s.id},utilitarian,neo-brutalism">Compare this direction</a><a class="button secondary" href="${s.links.brief}" download="${s.id}-brief.md">Download build brief</a></div></div><div class="columns"><div><section class="panel"><h2>What defines it</h2>${list(s.cues)}</section><section class="panel"><h2>What it asks of the project</h2>${facts(s)}</section><section class="panel"><h2>Color in use</h2><div class="proof" style="background:${s.tokens.color.surface};color:${s.tokens.color.text}"><strong>${e(s.cues[0])}</strong><p>${e(s.example)}</p><span style="background:${s.tokens.color.accent};color:${s.tokens.color.onAccent}">Accent label</span></div><p class="muted">Opaque text/surface: ${contrast(s.tokens.color.surface,s.tokens.color.text).toFixed(2)}:1 · Text/accent: ${contrast(s.tokens.color.accent,s.tokens.color.onAccent).toFixed(2)}:1.</p><p class="muted">These pairs meet the normal-text minimum; <a href="https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html">check other surfaces separately</a>.</p></section><section class="panel"><h2>Context</h2><p>${e(s.referenceNote)}</p>${s.source?`<p><a href="${s.source.url}">${e(s.source.label)}</a></p>`:''}<a href="/projects/territories/#style=${s.id}">View collected reference images</a></section></div><div><section class="panel"><h2>Build it</h2>${list(s.steps,'ol')}<h3>Typography</h3><p>${e(s.typeAdvice)}</p><h3>Watch out for</h3><p>${e(s.avoid)}</p></section><section class="panel"><h2>A working interpretation</h2><p>Original CSS composition with the same content and controls used in every direction. Image-led styles need additional artwork for their full expression.</p><a class="sample-link" href="${s.links.example}">Open the full example →</a><iframe class="full-example" src="${s.links.example}" title="${e(s.name)} interactive example" loading="lazy"></iframe></section><section class="panel"><h2>Hand it to an agent</h2><p>Point your agent at <a href="${s.links.guide}">this guide</a>, or save the files below.</p><div class="download-list"><a href="${s.links.guide}" download="${s.id}-guide.md">Guide</a><a href="${s.links.tokens}" download="${s.id}-tokens.json">Tokens JSON</a><a href="${s.links.brief}" download="${s.id}-brief.md">Build brief</a><a href="${s.links.checks}" download="${s.id}-checklist.md">Checklist</a></div></section><section class="panel"><h2>Check the result</h2>${list(s.checks.slice(0,4))}<a href="${s.links.checks}">Full acceptance checklist</a></section></div></div>`));}
 write('territories/start/index.html',shell('Find a direction','/territories/start/',`<div class="intro"><p class="eyebrow">Start with the work</p><h1>What are you making?</h1><p>A few preferences are enough to narrow the field. These are starting points; every style stays available.</p></div><form id="direction-form" class="panel">${fields}<div class="actions"><button type="submit">Find three directions</button></div></form><p id="picker-status" class="status" role="status"></p><div id="direction-results" class="results"></div><div id="compare-shortlist"></div><noscript><p class="nojs">The picker uses a small local script. You can also <a href="/territories/compare/">compare three starting directions</a> or <a href="/projects/territories/">browse all styles</a>.</p></noscript>`, 'choose.js'));
 const defaults=['utilitarian','japandi','neo-brutalism'];
 write('territories/compare/index.html',shell('Compare directions','/territories/compare/',`<div class="intro"><p class="eyebrow">Same content. Different decisions.</p><h1>See what changes.</h1><p>Compare spacing, type, color, and detail. Search the sample content and try the form to inspect empty, error, and success states.</p></div><label class="field" style="max-width:320px"><span>Example project</span><select id="compare-project">${projectSelect.replace('value="internal-tool"','value="internal-tool" selected')}</select></label><div class="preview-controls"><label class="field">Navigation<select id="compare-navigation"><option value="top">Top navigation</option><option value="sidebar">Left sidebar</option><option value="tabs">Centered tab bar</option></select></label><label class="field">Viewport<select id="compare-device"><option value="desktop">Desktop · 1440 px</option><option value="tablet">Tablet · 820 px</option><option value="mobile">Mobile · 390 px</option></select></label></div><p class="muted">Previews scale to fit; their layout uses the selected viewport width. Scroll inside a preview to explore it. Original UI studies, not historical reproductions. No information entered in the examples is sent or stored.</p><div class="comparisons">${defaults.map((id,i)=>{const s=styles.find(s=>s.id===id);return `<section class="comparison" data-slot="${i}"><label class="field"><span>Direction ${i+1}</span><select class="compare-style">${choices(id)}</select></label><p class="comparison-description muted">${e(s.profile.imagery)}</p><div class="device-preview"><iframe src="${s.links.example}" title="${e(s.name)} example" loading="lazy"></iframe></div><div class="actions"><a class="comparison-guide" href="${s.links.page}">Read guide</a><a class="comparison-example" href="${s.links.example}">Full example</a><a class="comparison-brief" href="${s.links.brief}" download="${s.id}-brief.md">Download brief</a></div><button class="copy-brief secondary" type="button">Copy build brief</button><p class="copy-status muted" role="status"></p></section>`;}).join('')}</div><div class="actions"><button id="copy-comparison" type="button">Copy comparison link</button><a href="/territories/start/">Start with your project</a></div><p id="comparison-status" role="status"></p><noscript><p class="nojs">Interactive selection needs JavaScript. The three examples and downloadable briefs above remain available.</p></noscript>`, 'compare.js'));
 write('territories/agents/index.html',shell('For agents','/territories/agents/',`<div class="intro"><p class="eyebrow">A public design reference</p><h1>Give your agent a direction.</h1><p>Send a guide URL, download a brief, or install the optional skill. Everything is a static file; there is no account or API key.</p><div class="actions"><a class="button" href="/territories/agent.md">Read agent instructions</a><a class="button secondary" href="/territories/territories-design.zip" download>Download skill</a></div></div><div class="columns"><div><section class="panel"><h2>Just send a link</h2><p>Ask your agent to read:</p><p class="code">${origin}/territories/agent.md</p><p>Or choose a style first and send its guide. During local review, use the preview host instead of the public domain.</p></section><section class="panel"><h2>Optional skill</h2><p>The skill helps an agent choose, implement, and check a direction. It adds instructions, not new tools.</p><p>Unzip the download and place the <code>territories-design</code> folder in your agent's supported skills location. No installer or configuration changes run automatically.</p><a href="/territories/skill/SKILL.md">Read the skill before installing</a></section></div><div><section class="panel"><h2>Files an agent can read</h2><ul><li><a href="/territories/catalog.json">Catalog JSON</a> — all styles, tradeoffs, and resource URLs.</li><li><a href="/territories/utilitarian/guide.md">Example guide</a> — practical interpretation.</li><li><a href="/territories/utilitarian/tokens.json">Example tokens</a> — semantic implementation values.</li><li><a href="/territories/utilitarian/checklist.md">Example checklist</a> — observable acceptance criteria.</li><li><a href="/territories/guides.md" download>All guide text</a> — a single downloadable reference.</li></ul></section><section class="panel"><h2>A small, explicit contract</h2><p>Schema ${version}. <a href="/territories/schema.json">Token schema</a>. Revision <code>${revision}</code>.</p><p>Current URLs follow the latest compatible content. The <code>/territories/v1/</code> paths retain the v1 structure; revision identifies editorial updates. Record it alongside downloaded files when you need to reproduce a decision.</p><p>Any agent with web-fetching access can read these resources. Downloads cover agents without it.</p></section></div></div>`));
}
if(process.argv[1]===fileURLToPath(import.meta.url))generateTerritories();
